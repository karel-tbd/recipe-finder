<a href="{{ path }}"
   class="flex items-center p-2 hover:text-gray-600 text-lg group {% if (app.request.pathInfo == path) or (app.request.attributes.get('_route') in active) %}text-white{% endif %}"
   aria-label="Sidebar">
    <span class="ms-3">{{ label }}</span>
</a>

