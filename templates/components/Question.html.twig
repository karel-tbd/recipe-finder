<div>
    <div class="absolute inset-0 flex items-center justify-center">
        <twig:Subtitle class="bg-gray-200 bg-opacity-70 rounded-xl px-4 py-2"
                       label="{{ label }}"/>
    </div>
    <div class="absolute inset-0 flex flex-wrap items-center justify-center gap-3 h-fit w-fit top-[55%] mx-auto">
        {% block content %}
        {% endblock %}
    </div>
</div>
