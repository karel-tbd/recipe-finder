<a class="rounded-xl w-fit h-fit px-4 py-2 border bg-gray-200 hover:bg-opacity-80 bg-opacity-70 inset-0 flex top-10 z-10 cursor-pointer whitespace-nowrap animate-bounce"
   data-position="{{ dataPosition }}"
   data-position-target="{{ dataTarget }}">
    <h2 class="text-xl lg:text-3xl">
        {% block content %}
        {% endblock %}
    </h2>
</a>
