{% extends 'base.html.twig' %}

{% block title %}Show recipe{% endblock %}

{% block body %}
    <div class="lg:grid lg:grid-cols-2">
        <img class="object-cover w-1/2 ml-10 rounded-xl" src="{{ vich_uploader_asset(recipe, 'image') }}" alt="{{ recipe.name }}">
        <div>
            <h2 class="text-3xl font-bold py-2 lg:w-3/4">{{ recipe.name }}</h2>
            <p class="lg:w-3/4 pt-5">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusantium, atque beatae commodi consequatur consequuntur cumque doloremque eos et eum fugit id labore minima, modi nihil optio porro, quaerat recusandae sapiente sed sint! Aspernatur ipsam itaque laborum nemo odio porro, quod.</p>
            <div class="grid grid-cols-3 mt-5 pb-5">
                <div class="text-center border-r">
                    <div class="w-8 m-auto  text-gray-500">{{ ux_icon('clock-regular') }}</div>
                    <p class="py-2 font-semibold text-lg">Total Time</p>
                    <p>{{recipe.time}} min</p>
                </div>

                <div class="text-center border-r">
                    <div class="w-6 m-auto  text-gray-500">{{ ux_icon('fire') }}</div>
                    <p class="py-2 font-semibold text-lg">Total Calories</p>
                    <p>734 cal</p>
                </div>

                <div class="text-center">
                    <div class="w-8 m-auto  text-gray-500">{{ ux_icon('user-group') }}</div>
                    <p class="py-2 font-semibold text-lg">Yield</p>
                    <p>Serves 4</p>
                </div>
            </div>
            <hr>
            <div class="flex justify-between w-full mt-10">
                <p>Created by <span class="text-red-500">{% if recipe.createdBy == null %}From Fridge to Feast{% endif %}{{ recipe.createdBy }}</span></p>
                <div class="flex gap-x-5">
                    <a href="#" title="Save recipe" class="w-4 text-gray-500">{{ ux_icon('bookmark-regular') }}</a>
                    <a href="" title="pdf" class="w-5  text-gray-500">{{ ux_icon('print') }}</a>
                </div>
            </div>
        </div>
    </div>

    <div class="flex w-4/5 m-auto gap-x-20 pt-20">
        <div class="w-2/3">
            <twig:Subtitle
                tag="h3"
                class="font-semibold pb-10"
                label="How to make it"/>
            <div class="recipe">
                <i class="fa-circle-check"></i>
                {{ recipe.description|replace({'</h2>' : '</h2> <hr>', '<p>': '<p class="text-gray-700">', '<h2>': '<h2 class="uppercase tracking-wide	">'})|raw }}
            </div>

        </div>
        <div class="w-1/3">
            <twig:Subtitle
                    tag="h3"
                    class="font-semibold"
                    label="Ingredients"/>
            <ul class="pt-5">
                {% for ingredient in recipe.ingredients %}
                    <li class="py-4 border-b text-gray-700 list-none pl-3">
                        {% if ingredient.id in searchIngredients %}
                            <div class="bg-green-400">
                                {{ ingredient.name }}
                            </div>
                        {% else %}
                            {{ ingredient.name }}
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>

    <twig:Link
        href="{{ path('recipe_edit', {'uuid':recipe.uuid}) }}"
        label="Edit recipe"/>
{% endblock %}
